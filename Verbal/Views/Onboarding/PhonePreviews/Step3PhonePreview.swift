import SwiftUI

struct Step3PhonePreview: View {
    var body: some View {
        Text("Step 3")
            .font(.title2.weight(.semibold))
            .foregroundStyle(Color(.mainText))
            .frame(maxWidth: .infinity, maxHeight: .infinity)
            .background(Color(.homeBackground))
    }
}
