import SwiftUI

struct Step2PhonePreview: View {
    var body: some View {
        Text("Step 2")
            .font(.title2.weight(.semibold))
            .foregroundStyle(Color(.mainText))
            .frame(maxWidth: .infinity, maxHeight: .infinity)
            .background(Color(.homeBackground))
    }
}
